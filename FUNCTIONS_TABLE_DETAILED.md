# Таблица реализации функций проекта "Виртуальное казино"

## Модуль базы данных (database/db.js) - Класс CasinoDatabase

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `constructor()` | Конструктор класса. Инициализирует путь к файлу базы данных SQLite | `this.dbPath` | Путь к файлу casino.db |
| Система | `init()` | Инициализирует базу данных: создает подключение, таблицы и заполняет начальными данными | `this.db` | Объект подключения к SQLite |
| Система | `createTables()` | Создает все таблицы БД: roles, permissions, players, game_categories, games, game_rounds и индексы | `this.db` | Объект БД для выполнения SQL |
| Система | `insertDefaultData()` | Заполняет БД начальными данными: роли (Игрок, Стример, Админ), права, категории игр, игры, тестовые пользователи | `roles`, `categories`, `games`, `testUsers`, `hashedPass` | Данные для инициализации |
| Система | `getUser(nickname, password)` | Аутентификация пользователя. Проверяет никнейм и пароль, возвращает пользователя с ролью и правами | `nickname`, `password`, `user`, `role`, `permissions`, `passwordMatch` | Данные для входа |
| Система | `createUser(nickname, password)` | Регистрация нового пользователя. Хеширует пароль, создает запись с балансом 10000 | `nickname`, `password`, `hashedPass`, `result`, `existing` | Данные для регистрации |
| Система | `getUserById(userId)` | Получает пользователя по ID вместе с ролью и правами доступа | `userId`, `user`, `role`, `permissions` | ID пользователя |
| Система | `updateBalance(userId, newBalance)` | Обновляет баланс пользователя в базе данных | `userId`, `newBalance` | ID и новый баланс |
| Система | `addGameRound(userId, gameId, amount)` | Добавляет запись об игровом раунде и автоматически обновляет баланс пользователя | `userId`, `gameId`, `amount`, `user`, `newBalance`, `result` | Данные игрового раунда |
| Система | `getUserStats(userId)` | Рассчитывает статистику пользователя: количество игр, суммарный результат, средний результат | `userId`, `stats` | ID для расчета статистики |
| Система | `getBalanceHistory(userId, limit)` | Получает историю изменения баланса с расчетом баланса на каждый момент времени | `userId`, `limit`, `rounds`, `history`, `runningBalance` | ID и лимит записей |
| Система | `getTransactionHistory(userId, limit)` | Получает историю транзакций (пополнений и выводов) с типом операции | `userId`, `limit`, `transactions` | ID и лимит записей |
| Система | `getAllUsers()` | Получает список всех пользователей системы с их ролями | `users` | Массив пользователей |
| Система | `updateUserRole(userId, roleId)` | Изменяет роль пользователя (для админов) | `userId`, `roleId` | ID пользователя и новая роль |
| Система | `getGameIdByName(gameName)` | Получает ID игры по её названию для связи с раундами | `gameName`, `game` | Название игры |

## Модуль главного процесса (main.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `createWindow()` | Создает главное окно Electron с настройками размера, стиля и загрузки HTML | `mainWindow`, `indexPath`, `width`, `height` | Параметры окна |
| Система | `app.whenReady()` | Обработчик готовности приложения. Инициализирует БД и создает окно | `db`, `mainWindow` | База данных и окно |
| Система | IPC: `db:getUser` | IPC обработчик для получения пользователя из рендер-процесса | `event`, `nickname`, `password` | Данные запроса |
| Система | IPC: `db:createUser` | IPC обработчик для создания нового пользователя | `event`, `nickname`, `password` | Данные регистрации |
| Система | IPC: `db:updateBalance` | IPC обработчик для обновления баланса | `event`, `userId`, `newBalance` | Данные баланса |
| Система | IPC: `db:addGameRound` | IPC обработчик для добавления игрового раунда | `event`, `userId`, `gameId`, `amount` | Данные раунда |
| Система | IPC: `db:getUserStats` | IPC обработчик для получения статистики пользователя | `event`, `userId` | ID пользователя |
| Система | IPC: `db:getBalanceHistory` | IPC обработчик для получения истории баланса | `event`, `userId` | ID пользователя |
| Система | IPC: `db:getTransactionHistory` | IPC обработчик для получения истории транзакций | `event`, `userId` | ID пользователя |
| Система | IPC: `db:getGameIdByName` | IPC обработчик для получения ID игры по названию | `event`, `gameName` | Название игры |
| Система | IPC: `window:minimize` | IPC обработчик для минимизации окна | - | - |
| Система | IPC: `window:maximize` | IPC обработчик для максимизации/восстановления окна | - | - |
| Система | IPC: `window:close` | IPC обработчик для закрытия окна | - | - |

## Модуль интерфейса (js/app.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `initializeApp()` | Главная функция инициализации. Проверяет элементы DOM и запускает все модули | `required`, `missing` | Список необходимых элементов |
| Система | `initWindowControls()` | Инициализирует кнопки управления окном (минимизация, максимизация, закрытие) | `minimizeBtn`, `maximizeBtn`, `closeBtn` | Кнопки управления |
| Система | `initAuth()` | Инициализирует систему авторизации: переключение вкладок, обработка входа и регистрации | `loginBtn`, `registerBtn`, `tabBtns`, `nickname`, `password`, `errorDiv` | Элементы формы авторизации |
| Система | `initLobby()` | Инициализирует главное меню: выбор игр, переход в профиль и настройки | `gameCards`, `profileBtn`, `settingsBtn`, `backBtns` | Элементы лобби |
| Система | `initProfile()` | Инициализирует экран профиля: кнопки пополнения и вывода средств | `depositBtn`, `withdrawBtn`, `backBtn` | Кнопки профиля |
| Система | `loadProfile()` | Загружает данные профиля: статистику, графики баланса и транзакций из БД | `stats`, `history`, `transactions`, `balanceChart`, `transactionChart` | Данные профиля |
| Система | `updateBalanceChart(history)` | Создает/обновляет график изменения баланса пользователя с помощью Chart.js | `history`, `balanceChart`, `labels`, `balances`, `ctx` | Данные для графика |
| Система | `updateTransactionChart(transactions)` | Создает/обновляет график трат и пополнений по дням | `transactions`, `transactionChart`, `dailyData`, `labels`, `deposits`, `withdrawals` | Данные транзакций |
| Система | `initTransactions()` | Инициализирует модальное окно пополнения/вывода средств | `closeBtn`, `confirmBtn`, `amount`, `method`, `isDeposit` | Элементы формы транзакций |
| Система | `showTransactionModal(type)` | Показывает модальное окно для пополнения или вывода средств | `type`, `screen`, `title` | Тип транзакции |
| Система | `showScreen(screenId)` | Переключает отображаемый экран приложения (скрывает все, показывает выбранный) | `screenId` | ID экрана для отображения |
| Система | `updateUI()` | Обновляет интерфейс: баланс во всех местах, никнейм пользователя | `balanceElements`, `nicknameElements`, `currentUser` | Элементы UI для обновления |
| Система | `formatMoney(amount)` | Форматирует сумму денег с учетом выбранной валюты и курса | `amount`, `currency`, `symbol`, `rate`, `symbols` | Сумма для форматирования |
| Система | `loadSettings()` | Загружает настройки пользователя из localStorage и заполняет форму настроек | `saved`, `userSettings` | Сохраненные настройки |
| Система | `setupSettingsHandlers()` | Устанавливает обработчики событий для всех элементов настроек | Различные элементы формы | Элементы настроек |
| Система | `saveSettings()` | Сохраняет все настройки пользователя в localStorage и применяет их | `userSettings` | Объект настроек |
| Система | `resetSettings()` | Сбрасывает настройки к значениям по умолчанию | `userSettings` | Настройки по умолчанию |
| Система | `applyTheme(theme)` | Применяет выбранную тему оформления к интерфейсу | `theme` | Название темы |
| Система | `applyFontSize(size)` | Применяет выбранный размер шрифта к интерфейсу | `size` | Размер шрифта |

## Модуль слотов (js/games/slots.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `initSlotsGame()` | Инициализирует игру в слоты: настройка слайдера ставки, кнопки спина, создание барабанов | `spinBtn`, `betSlider`, `betAmount`, `reels`, `symbol`, `user` | Элементы интерфейса слотов |
| Система | `spinSlots(bet)` | Выполняет спин барабанов: анимация вращения, генерация финальных результатов, списание ставки | `bet`, `isFreeSpin`, `finalResults`, `spinDuration`, `spinSpeed`, `intervalId`, `elapsed`, `reels` | Параметры спина |
| Система | `finishSpin(bet, isFreeSpin, finalResults)` | Завершает спин: рассчитывает выигрыш по комбинациям, обновляет баланс, показывает результат | `bet`, `isFreeSpin`, `finalResults`, `winAmount`, `newFreespins`, `line`, `symbol`, `multiplier` | Результаты спина |

## Модуль блэкджека (js/games/blackjack.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `initBlackjackGame()` | Инициализирует игру в блэкджек: настройка кнопок, слайдера ставки, загрузка статистики | `dealBtn`, `hitBtn`, `standBtn`, `betSlider`, `betAmount`, `user` | Элементы интерфейса блэкджека |
| Система | `resetGame()` | Сбрасывает состояние игры: создает новую колоду, очищает карты игрока и дилера | `blackjackGame`, `deck` | Объект состояния игры |
| Система | `createDeck()` | Создает колоду из 52 карт (4 масти × 13 достоинств) и перемешивает её | `deck`, `suits`, `ranks`, `i`, `j` | Массивы мастей и достоинств |
| Система | `getCardValue(card)` | Возвращает числовое значение карты (туз = 11, фигуры J/Q/K = 10, остальные = номинал) | `card`, `rank` | Объект карты |
| Система | `calculateScore(cards)` | Рассчитывает сумму очков в руке с учетом туза (11 или 1, в зависимости от суммы) | `cards`, `score`, `aces`, `card` | Массив карт для расчета |
| Система | `displayCard(card, container, hidden)` | Отображает карту на экране (скрытую рубашкой или открытую с мастью и достоинством) | `card`, `container`, `hidden`, `cardDiv`, `isRed` | Данные карты и контейнер |
| Система | `dealCards()` | Раздает начальные карты игроку и дилеру (по 2 карты), списывает ставку | `bet`, `playerCards`, `dealerCards`, `playerScore`, `dealerScore`, `user` | Ставка и массивы карт |
| Система | `playerHit()` | Игрок берет еще одну карту из колоды | `card`, `playerCards`, `playerScore` | Новая карта и состояние |
| Система | `playerStand()` | Игрок останавливается, дилер берет карты до 17, определяется победитель | `dealerCards`, `dealerScore`, `card`, `card` | Карты дилера |
| Система | `endGame(message)` | Завершает игру: определяет победителя по правилам блэкджека, начисляет выигрыш, обновляет статистику | `result`, `winAmount`, `isWin`, `isLoss`, `playerBJ`, `dealerBJ` | Результат игры |
| Система | `loadBlackjackStats()` | Загружает статистику побед/поражений из localStorage | `saved`, `blackjackStats` | Сохраненная статистика |
| Система | `saveBlackjackStats()` | Сохраняет статистику побед/поражений в localStorage | `blackjackStats` | Статистика для сохранения |
| Система | `updateBlackjackStatsDisplay()` | Обновляет отображение счетчиков побед и поражений на экране | `winsEl`, `lossesEl` | Элементы отображения |

## Модуль рулетки (js/games/roulette.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `createRouletteNumbers(container)` | Создает номера на колесе рулетки с правильным порядком европейской рулетки и цветами | `container`, `rouletteOrder`, `numberDiv`, `number`, `index`, `angle` | Контейнер и массив номеров |
| Система | `initRouletteGame()` | Инициализирует игру в рулетку: создает номера, настраивает кнопки и слайдеры, загружает статистику | `spinBtn`, `betSlider`, `betAmount`, `betOptions`, `wheelInner`, `user` | Элементы интерфейса рулетки |
| Система | `spinRoulette(bet)` | Запускает вращение рулетки: анимация колеса, генерация выигрышного номера, списание ставки | `bet`, `winningNumber`, `isRed`, `isBlack`, `isGreen`, `spinDuration`, `finalAngle`, `currentAngle` | Параметры спина |
| Система | `finishRouletteSpin(number, isRed, isBlack, isGreen, isEven, isOdd, bet)` | Завершает спин: проверяет выигрыш по типу ставки, обновляет баланс и статистику | `number`, `win`, `multiplier`, `winAmount`, `color` | Результат спина |
| Система | `highlightWinningNumber(number)` | Подсвечивает выигрышный номер на колесе золотым цветом с анимацией | `number`, `winningEl` | Выигрышный номер |
| Система | `loadRouletteStats()` | Загружает статистику побед/поражений из localStorage | `saved`, `rouletteStats` | Сохраненная статистика |
| Система | `saveRouletteStats()` | Сохраняет статистику побед/поражений в localStorage | `rouletteStats` | Статистика для сохранения |
| Система | `updateRouletteStatsDisplay()` | Обновляет отображение счетчиков побед и поражений на экране | `winsEl`, `lossesEl` | Элементы отображения |

## Модуль Streamer Mode (js/streamer-mode.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `initStreamerMode()` | Инициализирует Streamer Mode: проверяет права пользователя, настраивает кнопку активации | `streamerBtn`, `closeOverlayBtn`, `currentUser` | Элементы Streamer Mode |
| Система | `toggleStreamerMode()` | Переключает режим Streamer Mode ON/OFF, показывает/скрывает overlay | `streamerModeActive`, `overlay`, `streamerBtn` | Состояние режима |
| Система | `startStreamerUpdates()` | Запускает автообновление overlay каждые 2 секунды | `streamerUpdateInterval` | Интервал обновления |
| Система | `stopStreamerUpdates()` | Останавливает автообновление overlay при выключении режима | `streamerUpdateInterval` | Интервал для очистки |
| Система | `updateStreamerOverlay()` | Обновляет все данные в debug overlay: баланс, игры, статистика | `currentUser` | Текущий пользователь |
| Система | `updateBalanceInfo()` | Обновляет информацию о балансе в overlay: текущий баланс и изменение после последней игры | `currentBalance`, `balanceChange`, `sessionStats` | Данные баланса |
| Система | `updateRecentGames()` | Обновляет список последних 5 игр в overlay с иконками, названиями, временем и суммами | `history`, `gamesMap`, `round`, `gameInfo`, `timeStr` | История игр |
| Система | `updateSessionStats()` | Обновляет статистику сессии: количество игр, суммарные выигрыши/проигрыши, чистый результат | `sessionStats`, `gamesPlayed`, `totalWins`, `totalLosses`, `netResult` | Статистика сессии |
| Система | `formatMoneyForStreamer(amount)` | Форматирует деньги для Streamer Mode (всегда в долларах $) | `amount` | Сумма для форматирования |
| Система | `updateStreamerStats(gameAmount)` | Обновляет статистику сессии после каждой игры | `gameAmount`, `sessionStats` | Сумма игры |

## Модуль чата поддержки (js/support-chat.js)

| Ответственный | Название метода | Описание метода | Использованные переменные | Назначение переменных |
|--------------|----------------|-----------------|---------------------------|----------------------|
| Система | `initSupportChat()` | Инициализирует чат поддержки: кнопки сворачивания, отправки сообщений, обработчики | `toggleBtn`, `sendBtn`, `chatInput`, `chatWidget`, `isMinimized` | Элементы чата |
| Система | `sendMessage(text, isUser)` | Отправляет сообщение в чат (от пользователя или бота) и прокручивает к последнему | `text`, `isUser`, `messageDiv`, `messagesContainer` | Текст сообщения |
| Система | `getBotResponse(userMessage)` | Генерирует ответ бота на основе ключевых слов в сообщении пользователя | `userMessage`, `lowerMessage`, `botResponses`, `key` | Сообщение пользователя |

---

## Глобальные переменные и объекты

| Переменная | Тип | Описание | Использование |
|-----------|-----|----------|---------------|
| `currentUser` | Object | Текущий авторизованный пользователь с ролью и правами | Во всех модулях для доступа к данным пользователя |
| `userSettings` | Object | Настройки пользователя (валюта, тема, звук и т.д.) | В модуле настроек и форматирования денег |
| `balanceChart` | Chart | График изменения баланса (Chart.js) | В профиле пользователя |
| `transactionChart` | Chart | График трат и пополнений (Chart.js) | В профиле пользователя |
| `blackjackStats` | Object | Статистика побед/поражений в блэкджеке | В модуле блэкджека |
| `rouletteStats` | Object | Статистика побед/поражений в рулетке | В модуле рулетки |
| `sessionStats` | Object | Статистика сессии для Streamer Mode | В модуле Streamer Mode |
| `streamerModeActive` | Boolean | Флаг активности Streamer Mode | В модуле Streamer Mode |
| `slotsSpinning` | Boolean | Флаг вращения барабанов в слотах | В модуле слотов |
| `freespinsLeft` | Number | Количество оставшихся фриспинов | В модуле слотов |
| `rouletteSpinning` | Boolean | Флаг вращения рулетки | В модуле рулетки |
| `selectedBet` | String | Выбранный тип ставки в рулетке | В модуле рулетки |
| `blackjackGame` | Object | Состояние текущей игры в блэкджеке | В модуле блэкджека |

---

## Примечания

- Все функции используют современный JavaScript (ES6+)
- Асинхронные функции используют `async/await` для работы с базой данных через IPC
- Статистика игр сохраняется в `localStorage` для сохранения между сессиями
- IPC (Inter-Process Communication) используется для связи между главным процессом (Node.js) и рендер-процессом (браузер)
- Все функции имеют обработку ошибок через `try/catch`
- Графики используют библиотеку Chart.js версии 4.4.0
- База данных использует better-sqlite3 для синхронной работы с SQLite

